name: kafka-connect-datagen
lang: java
lang_version: 8
git:
  enable: true
codeowners:
  enable: true
semaphore:
  enable: true
  pipeline_type: cp
  cve_scan: true
  extra_deploy_args: '"-Dcloud"'
  extra_build_args: '"-Dcloud"'
